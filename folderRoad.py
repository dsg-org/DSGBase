import os
def folderRoad():
        base_path = os.path.dirname(os.path.abspath(__file__))
        return  base_path
# ამის დამატება მომიწია რადგან pysrc ში ფაილს სხვანაირად ხედავდა და სწორად ვერუთითებდა.
# რატქმაუნდა დროებითია